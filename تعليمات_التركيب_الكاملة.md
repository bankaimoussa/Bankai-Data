# دليل تركيب منظومة مندوبين التوصيل (Backend + Frontend)

المشروع بيتكون من جزئين:
- **backend/** — سيرفر Node.js بيتكلم مع Google Sheets API مباشرة، هترفعه على Render
- **frontend/** — الموقع نفسه (كروت، بحث، تسجيل دخول)، هترفعه على Vercel أو Netlify

---

## الجزء 1: تجهيز Google Sheets API (Service Account)

الطريقة دي أقوى من Apps Script لأنها بتديك API حقيقي تقدر تتحكم فيه بالكامل من السيرفر بتاعك.

1. روح على **https://console.cloud.google.com**
2. اعمل مشروع جديد (New Project) — سمّيه أي اسم زي `da-management`
3. من قائمة البحث فوق، دوّر على **Google Sheets API** وفعّله (Enable)
4. روح لـ **APIs & Services > Credentials**
5. اضغط **Create Credentials > Service Account**
6. اديله اسم زي `da-sheets-bot` واضغط **Create and Continue** ثم **Done**
7. هتلاقي الـ Service Account في القائمة — ادخل عليه
8. روح تبويب **Keys > Add Key > Create new key > JSON**
9. هينزلك ملف JSON — **احتفظ بيه كويس، ده مفتاح السيرفر بالكامل**
10. افتح الملف، هتلاقي جواه إيميل زي:
    `da-sheets-bot@da-management-xxxxx.iam.gserviceaccount.com`

## الجزء 2: إديه صلاحية على الشيت

1. افتح ملف Google Sheets بتاعك
2. اضغط **Share** (مشاركة)
3. الصق إيميل الـ Service Account اللي طلع فوق
4. اديله صلاحية **Editor**
5. احفظ

## الجزء 3: خد الـ Spreadsheet ID

من رابط الشيت:
```
https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUvWxYz/edit
                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ ده الـ ID
```

## الجزء 4: تأكد من شكل الشيت

- كل فرع في تاب (Sheet) منفصل، اسمه بالظبط `QCD1`, `QCD2` (أو أي أسماء تختارها)
- الصف الأول (Row 1) هو العناوين
- الأعمدة بالترتيب ده بالظبط من A إلى Q:
  ```
  A: Site Code | B: Vendor | C: DA Name | D: National ID | E: Email
  F: Transporter ID | G: Vehicle Type | H: Vehicle Plate | I: Model Type
  J: Phone | K: License Issuance | L: License Expiration | M: DOB
  N: License Number | O: License Type | P: Flex | Q: Address
  ```

---

## الجزء 5: رفع الباك اند على Render

1. اعمل حساب على **render.com** (مجاني)
2. ارفع مجلد `backend/` على GitHub repo (لو مش عارف تعمل ده قولّي هساعدك)
3. من Render Dashboard: **New > Web Service**
4. اربطه بالـ repo بتاعك
5. الإعدادات:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: Free
6. من قسم **Environment**، ضيف المتغيرات دي (Environment Variables):

   | Key | Value |
   |---|---|
   | `ADMIN_PASSWORD` | `bankai` |
   | `JWT_SECRET` | أي نص عشوائي طويل (مثلاً 40 حرف) |
   | `SPREADSHEET_ID` | الـ ID اللي أخدته فوق |
   | `BRANCHES` | `QCD1,QCD2` |
   | `GOOGLE_SERVICE_ACCOUNT_JSON` | محتوى ملف الـ JSON كله في سطر واحد |

   ملحوظة مهمة: عشان تحط الـ JSON في سطر واحد، افتح الملف وانسخ محتواه بالكامل والصقه كما هو (Render بياخده كنص عادي حتى لو فيه أسطر).

7. اضغط **Create Web Service**
8. بعد ما يخلص deploy، هتلاقي رابط زي: `https://da-backend-xxxx.onrender.com`
9. جرب تفتح `https://da-backend-xxxx.onrender.com/api/health` — المفروض يديك `{"status":"ok"}`

**ملحوظة**: الخطة المجانية في Render بتنام بعد فترة من غير استخدام، وأول طلب بعد النوم بياخد 30-50 ثانية يصحى. لو عايز يفضل صاحي دايمًا فيه خطط مدفوعة تبدأ من 7$ شهريًا.

---

## الجزء 6: ربط الفرونت اند بالباك اند

1. افتح ملف `frontend/config.js`
2. غيّر السطر:
   ```js
   const API_BASE = "PASTE_YOUR_BACKEND_URL_HERE";
   ```
   إلى:
   ```js
   const API_BASE = "https://da-backend-xxxx.onrender.com";
   ```
3. احفظ

---

## الجزء 7: رفع الفرونت اند

### الطريقة الأسهل: Netlify Drop
1. روح **netlify.com/drop**
2. اسحب مجلد `frontend/` بالكامل على الصفحة
3. هياخد لينك فورًا زي `https://xxxx.netlify.app`

### أو Vercel
1. اعمل حساب على **vercel.com**
2. اربطه بـ GitHub repo فيه مجلد `frontend/`
3. اضغط Deploy

---

## تسجيل الدخول للموقع

كلمة المرور: `bankai`

---

## ملاحظات مهمة

- **المزامنة**: الموقع بيحدّث البيانات تلقائيًا كل 4 ثواني من الشيت، فأي تعديل تعمله يدويًا في الشيت هيظهر في الموقع خلال ثواني قليلة. وأي تعديل تعمله من الموقع بينزل في الشيت فورًا.
- **إضافة فرع جديد**: ضيف اسم الشيت الجديد في متغير `BRANCHES` في Render (مفصول بفاصلة)، وأضف تاب بنفس الاسم في الشيت.
- **تغيير الباسورد**: غيّر قيمة `ADMIN_PASSWORD` في Render وأعد الـ deploy.
- **الأمان**: الباسورد والمفاتيح كلها في Environment Variables على السيرفر مش في الكود، فمينفعش حد يشوفهم من الموقع.

---

## لو محتاج مساعدة في أي خطوة

قولّي وهساعدك خطوة بخطوة، خصوصًا في:
- رفع المشروع على GitHub لأول مرة
- ضبط إعدادات Render بالتفصيل
- حل أي خطأ يظهرلك أثناء التركيب
